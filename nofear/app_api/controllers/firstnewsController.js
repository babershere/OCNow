const axios = require('axios');
const db = require('../models');

// Defining methods for the firsnewsController
// findAll searches the firstnewsController API and returns only the entries we haven't already saved
//API call it here
module.exports = {
    findAll: function(req, res) {

    }
}